import { GetServerSideProps, NextPage } from "next";

const Single: NextPage = () => {
  return <p>You're on Single Page</p>;
};

// export const getServerSideProps: GetServerSideProps = (context) => {};
export default Single;
